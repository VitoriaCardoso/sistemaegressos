# Nome da aplica��o
spring.application.name=sistemaegressos

server.port=8080

# Configura��o do banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/sistemaegresso
spring.datasource.username=postgres
spring.datasource.password=Vitoria@20022001

# Dialeto do PostgreSQL para o Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jackson.date-format=yyyy/MM/dd
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

# Defini��o de cria��o do schema (Evita que o Hibernate crie ou altere as tabelas)

# Configura��o para execu��o dos scripts SQL
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:db/schema.sql
#spring.sql.init.data-locations=classpath:db/data.sql

# Ativar logs detalhados do Hibernate e SQL
logging.level.org.springframework.jdbc=DEBUG
logging.level.org.hibernate.SQL=DEBUG

oauth.govbr.client-id=SEU_CLIENT_ID
oauth.govbr.client-secret=SEU_CLIENT_SECRET
oauth.govbr.redirect-uri=http://localhost:8080/oauth/callback
oauth.govbr.authorize-uri=https://sso.acesso.gov.br/authorize
oauth.govbr.token-uri=https://sso.acesso.gov.br/token


